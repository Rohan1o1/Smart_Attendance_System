Todo: Commit all changes to deployment branch

1. Inspect git status and current branch
2. Checkout `deployment` branch if not already on it
3. Stage all changes and commit with message
4. Optionally push (not done unless requested)
